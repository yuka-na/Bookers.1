<% if flash[:notice] %>
  <p class="success"><%= flash[:notice] %></p>
  <% end %>
  <div class="show">
	<h4 class="show1"><strong>Title:</strong><%= @book.title %></h4>
	<h4 class="show1"><strong>Body:</strong><%= @book.body %></h4>
	<h5><%= link_to "Edit", edit_book_path(@book.id),class: "edit_#{book.id}" %>|<%= link_to "Back", new_book_path(@book.id),class: "new_#{book.id}" %></h5>
</div>